<div class="user-admin-data-form row">
<%= form_with(model: administrator, local: true) do |form| %>
  <% if administrator.errors.any? %>
    <div class="error-explanation">
      <h2><%= pluralize(administrator.errors.count, "error") %> prohibited this administrator from being saved:</h2>

      <ul>
      <% administrator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= form.text_field :username, placeholder: 'Username' %>
    <h5>Username can contain any letter and an underscore</h5>
  </p>
  
  <p><%= form.text_field :name, placeholder: 'Full name' %></p>
  <p><%= form.text_field :email, placeholder: 'Email Address' %></p>
  <p><%= form.password_field :password, placeholder: 'Password' %></p>
  <h5>Must contain a lowercase character, an uppercase character, a digit and a special character</h6>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>