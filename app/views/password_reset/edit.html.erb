<% provide(:button_text, 'Reset your password') %>
<%= form_for (@user, url: new_password_path(params[:id])) do |f| %>
    <%= hidden_field_tag :email, @user.email %>
 <p><%= f.password_field :password %></p>
 <p><%= f.submit yield(:button_text) %></p>
<% end %>